# PDF.js runtime

The bundled `pdf.min.js` and `pdf.worker.min.js` files come from
`pdfjs-dist@3.11.174`. They are served locally so the PDF reader, page jumps,
and outline synchronization continue to work in the Windows one-file build
without relying on a CDN.

The upstream Apache-2.0 license is preserved in `LICENSE`.
